"""Встроенная система обновлений Bot4VPS (4.0+).

Состав:
- updater.py   — логика в процессе: проверка версии по changelog из main,
                 состояние data/update/state.json, запуск runner'а;
- runner.py    — standalone-скрипт вне процесса: скачивание, backup, замена
                 кода, pip, перезапуск, health-check, автороллбек;
- scheduler.py — суточная задача проверки (PTB JobQueue).

Правило: main — единственный источник для проверки и обычного обновления;
GitHub Releases — только источник конкретной версии при откате.
"""
